# ความปลอดภัยเมื่อ Google Sheet มีหลายคนใช้งาน

เว็บนี้ถูกออกแบบเป็น **personal sidecar** — เป็นหน้ามองข้อมูลส่วนตัว ไม่ใช่ฐานข้อมูลใหม่ และไม่เปลี่ยนวิธีทำงานของคนอื่นในชีต

## สิ่งที่เว็บตั้งใจไม่ทำ
- ไม่ sort แถวบน Google Sheet
- ไม่ insert/delete/move แถว
- ไม่เปลี่ยน format
- ไม่แก้สูตร
- ไม่ update ทั้ง row
- ไม่ใช้ Service Account ที่มีสิทธิ์เหนือผู้ใช้
- ไม่เก็บ Access Token ใน localStorage/sessionStorage
- ไม่ฝังข้อมูลลูกค้าจริงใน source code

## วิธีลดการเขียนทับคนอื่น
เมื่อกดบันทึก เว็บจะ:
1. โหลดชีตล่าสุดใหม่
2. หาแถวจาก fingerprint ของข้อมูลที่ถูกล็อก เช่น LINE, ชื่อ, เบอร์, หมวด, สินค้า, ราคา, วันที่รับ, ชื่อเพจ, ลิงก์เพจ
3. ต้องพบ **เพียง 1 แถว** เท่านั้น
4. เปรียบเทียบค่าปัจจุบันของแต่ละเซลล์ที่จะเปลี่ยนกับค่าตอนเปิด drawer
5. ถ้าไม่ตรง แสดง Conflict และไม่บันทึก
6. ถ้าตรง เขียนเฉพาะเซลล์ที่เปลี่ยน

## ข้อจำกัด
Sheets API ไม่มี transaction/compare-and-swap สำหรับ value update จาก browser ดังนั้นถ้ามีผู้ใช้แทรกแถวพอดีในช่วงสั้นมากระหว่างตรวจล่าสุดกับคำสั่งเขียน ยังมี race condition ได้

ถ้าภายหลังต้องการความเข้มงวดระดับ transaction จริง แนะนำเพิ่ม Apps Script/Backend ที่ใช้ LockService และตรวจ fingerprint ฝั่ง server ก่อนเขียน แต่สำหรับผู้ใช้คนเดียวที่ต้องการหน้าใช้ง่ายขึ้นบนชีตกลาง โครงสร้างปัจจุบันรบกวนระบบเดิมน้อยที่สุด
